namespace NumberTron {
  class Runner {
    static void Main(string[] args) {

    }
  }

  class Player {
    string name;
    string number;

    Player (string name, int number) {
        this.name = name;
        this.number = number;
    }
  }

  class Board {
    
  }
}
